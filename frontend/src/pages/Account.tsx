import React from "react";

import { Helmet } from "react-helmet";

const Account = () => {
  return (
    <div>
      <Helmet>
        <title>React Clinic | Account</title>
      </Helmet>
      <h2>Account</h2>
    </div>
  );
};

export default Account;
